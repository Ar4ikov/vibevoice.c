Closes #

## What was wrong

What the code did before this, and what it cost — a timing, a size, a wrong
transcript, an alert. Measured if it can be measured.

## The change

What it does now, as its user sees it: flags, formats, endpoints, behaviour.
Say what is deliberately left out.

## Verified

How you know. The machine, the input and the command, not "tested locally".
If the output is supposed to be unchanged, show that it is.

- [ ] `ctest` passes (say where — GPU box, MSVC, both)
- [ ] CHANGELOG entry under `## Unreleased`
- [ ] Docs updated if behaviour or flags changed
