# Python dependencies for offline model conversion tools
# These are NOT runtime dependencies — the C runtime is self-contained.

torch>=2.6          # CVE-2025-32434: torch.load(weights_only=True) RCE before 2.6
transformers>=4.51.3  # the version tools/compare_ref.py is written against; also past the 2024 deserialization CVEs
safetensors>=0.4
bitsandbytes>=0.43
onnx>=1.15
# tensorrt>=10.0  # Install separately via NVIDIA
numpy>=1.24
tqdm>=4.66.3        # CVE-2024-34062
